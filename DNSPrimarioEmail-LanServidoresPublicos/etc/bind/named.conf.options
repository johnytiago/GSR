acl goodclients {
        10.0.0.0/24;
        localhost;
        localnets;
};

options {
        directory "/var/cache/bind";

        recursion yes;
        allow-query { goodclients; };

        forwarders {
                10.0.0.6;
        };



        auth-nxdomain no;    # conform to RFC1035
        listen-on-v6 { any; };
};
